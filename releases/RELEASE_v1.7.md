# KAASA SmartOS v1.7 릴리스 노트

**빌드**: `kaasa_smartos_v1.7_40a800f.zip` (4.5MB · 988파일 · 41화면)
**기준 커밋**: 40a800f

## 이번 버전 핵심 (v1.6 → v1.7)

### ★ 평년 기상(ERA5) 인프라 — 20→26작물·4접점
- 무키 ERA5(Open-Meteo archive) 페처 → 26작물 월별 평년값(기온·일사·강수·GDD, 작물별 GDD 기준온도)
  - 온실6·노지채소9·과수5·주식고소득6(벼·고추·콩·감자·고구마·수박·사과·배·복숭아·감·포도 등)
- 4접점: **G2**(온실 냉난방)·**F3**(노지 작황)·**F8**(★광역 이상기상 작동진단)·**C13 챗봇**
- F8: 16일예보 vs 평년 편차(±3℃·강수±50%) → 광역 이상기상 시 이상필지 알림 심각도 상향

### AI 영농비서(C13) 정상화·고도화
- 공개데모 POST /chat 허용(읽기전용 게이트 유지) → 챗봇 작동
- 신규 의도: 평년기상·데이터 직접 입력 안내 / LLM 시스템프롬프트에 평년·진단·역량·작황·입력안내 통합

### 딸기 M2 모델 개선 (ERA5 보강)
- 외부기온 결측 보강 → LOYO CV R² 0.192→0.295, MAPE 20.8→17.8%, 게이트 통과
- (타작물은 측정 후 효과 없어 롤백 — 거짓향상 0)

### 품질·정합 (실측 검증, 회귀 0)
- 데이터 출처 버그 수정(F4 흙토람·F2 팜맵 실데이터 배지·C3 KAMIS 시세 폴백)
- 접근성 3축(레이블·터치타겟·색대비 AA) / 온보딩 동의 통합 / 네비게이터 재정리
- C20 데모 왜곡 교정(실관리자뷰 일치) / DecisionDeck 데모 정직성(거짓 성공 제거)
- **SW HTML network-first + 캐시 v3** — 묵은 화면(스테일 캐시) 방지

### 문서·배포
- `docs/USER_MANUAL.md`(상세 매뉴얼 17장) · `docs/DEMO_GUIDE_BY_CROP.md`(작물별 시연)
- 고정도메인 턴키 `deploy/cloudflare/setup_named_tunnel.ps1`
- 연합학습 스캐폴드(`pipeline/federated/`) + 전송 엔드포인트

## 구성 (zip 제외 항목)
모델 바이너리(.pkl)·parquet·원천 빅데이터(기초자료)·DB덤프·빌드산출물·캐시·git 제외.
→ 소스·화면·컴포넌트·API·스크립트·ERA5 평년데이터·문서 포함.

## 실행
```
PUBLIC_DEMO=1 JWT_SECRET_KEY=<랜덤> python -m uvicorn api.main:app --host 0.0.0.0 --port 8000
```
- `/intro` 소개 · `/smartos` 네비게이터 · 로그인 admin/1250(자동)
- 모델 아티팩트(.pkl)는 별도 배포 필요(zip 미포함).
