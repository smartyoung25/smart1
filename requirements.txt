fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
pg8000>=1.31.0
pandas>=2.0.0
pyarrow>=15.0.0
numpy>=1.26.0
# ★ pickle 호환 고정 — 아티팩트(models/artifacts/*.pkl)는 학습 환경 버전으로 직렬화된다.
#   상한이 없으면 재빌드 때 최신본이 깔려 unpickle 경고/무효 결과가 난다. 실제로 컨테이너가
#   sklearn 1.9.0 을 설치해 1.8.0 아티팩트와 어긋났다(InconsistentVersionWarning:
#   "might lead to breaking code or invalid results"). 학습 환경과 동일 버전으로 고정한다.
#   ※ 재학습으로 버전을 올릴 때 이 핀도 함께 올릴 것(학습=서빙 동기).
scikit-learn==1.8.0
xgboost==3.2.0
lightgbm==4.6.0
optuna>=3.6.0
apache-airflow>=2.9.0
mlflow>=2.12.0
redis>=5.0.0
python-multipart>=0.0.9
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.0
slowapi>=0.1.9
python-dotenv>=1.0.0
openpyxl>=3.1.0
