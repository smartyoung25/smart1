# 작업일지 — 2026-08-07

**주제**: 인터랙티브 E2E 감사 → 경제모델 SSOT 리팩터 → E2E 후속 이슈 전량 해소 → iwinv 운영 배포 → 온보딩·거래성 화면 보완
**결과**: 커밋 17개(코드 15 + 문서 2), pytest 12F/10P 회귀 0 유지, SW v81→**v97** 운영 반영 완료(3회 배포: v95·v96·v97)
**상세 보고서**: [E2E_분석보고서_2026-08-07.md](E2E_분석보고서_2026-08-07.md) · [SSOT_구현계획_2026-08-07.md](SSOT_구현계획_2026-08-07.md)

---

## 🌅 오전 (11:00–12:10) — E2E 감사 & 경제모델 SSOT 착수

**감사(커밋 전)**: 서브에이전트 없이 인터랙티브 E2E **26화면** 워크스루 — (A)UX 현실성/사용성 + (B)구현 로직 관점. 전 화면 콘솔 에러 0.
- **최상위 발견 = 경제모델 파편화**: 같은 farm_001 오이가 화면마다 흑자 +95% ↔ 적자 −18%, 작목 오이 ↔ 딸기, 수확량 42,005 ↔ 18,000kg. 근원 4개(수확량 3중 소스·매출 디커플·월/시즌 단위혼용·깨진 import).

| 시각 | 커밋 | 내용 |
|------|------|------|
| 11:26 | `dc6bee1` | 격리버그 3건(G4-1 `/erp` 딸기폴백·F4-2 관개량 10배·G3-4 Period NaN) + **경제 SSOT Phase1~4**: 수확량 단일함수(get_yield_forecast)·단가 통일·매출=수량×단가 파생·비용 시즌화 |
| 11:37 | `640eaef` | 오이 season_months 8→5 캘리브레이션(소득률 17%→48% RDA정합) + c5 배너 시즌비용 정정 |
| 11:44 | `adf6c28` | 비용 SSOT — `/erp/realtime` 원가·마진을 `_compute_costs` 기준 통일(/erp=/revenue 4/4 일치) |
| 11:52 | `24b39da` | 공동출하 채널 프레이밍 정직화(순수취 기준·조건부, 정산 공동단가 기준) — G6-3·C12-2·C12-1 |
| 12:10 | `c16abb3` | season_months 3곳 중복(model_loader·m2_yield·erp_calculator) → crop_config 단일화 |

## 🌇 오후 (13:13–14:44) — E2E 후속 이슈 순차 처리

| 시각 | 커밋 | 내용 |
|------|------|------|
| 13:13 | `3306c69` | VPD 밴드 개념 분리·통일 — 일반 적정대(0.8~1.2 고정) vs 작목×단계 최적(라벨 구분) + 오이 `_VPD_OPTIMAL` 누락 수정, c25 Plan 1.0~1.4→0.8~1.2 (G2-1·C4-2·C25-5) |
| 13:25 | `c00fffc` | 추천 ±섭동 노이즈 필터 — optimize() 정렬직전 선별(순이익>임계·제어가능·±중복 제거) (C4-1) |
| 13:31 | `e3aae7e` | 수확 D-day 2중계산 단일화 — `_days_to_harvest` 헬퍼로 /summary·/harvest 통일 (C4-3) |
| 13:43 | `873154c` | c4 종합진단 점수 라벨충돌 해소 — 상단 점수를 /diagnosis overall_score로 통일(66↔44) (C4-4) |
| 13:57 | `b573ca5` | PDCA(c25) 4대 이슈 — 근원 `_farm_meta` 딸기오인을 `_require_farm`로 통일 → 효율성/효과성/드리프트/EP 정합 (C25-1~4) |
| 14:07 | `e7817b9` | 관제(c20) NDVI 스텁·진단 상충 — vigor 좌편포 스프레드 + 진단을 작황과 상관 (C20-1/2) |
| 14:30 | `850d715` | 가입 동의 게이트 — 이용약관·개인정보 필수동의(미동의 시 PII 전송 차단) + 아이디·비번 필수표시 (C0-2·C0-1) |
| 14:44 | `d87b20f` | season 삼중상이 통합 — crop_config에 운영작기(비용·pdca)·페놀로지 주기(생육단계) 두 개념 분리·단일출처화 |

## 🚀 배포 & 마무리 (16시경)

- **iwinv 운영 배포**: 내 PC `git push origin master` → 서버 SSH → 코드경로만 `git checkout origin/master` → `docker compose build api && up -d api` → **2경로 대조 검증**.
  - 검증 결과: `iwinv: kaasa-smartos-v95` == `domain: kaasa-smartos-v95` → **farmingsight.org가 iwinv를 정확히 서빙**(터널 사고 없음) ✅
  - 배포 방식: 코드 checkout + 이미지 재빌드(Dockerfile COPY 베이크). docker-compose.yml·Dockerfile은 checkout 제외(서버 수동수정 보존).

| 시각 | 커밋 | 내용 |
|------|------|------|
| 16:26 | `13e2180` | CLAUDE.md SW 캐시 표기 v81→v95 정정 (문서 전용, 컨테이너 미포함이라 재배포 불필요) |

## 🔁 v96 재배포 — 구현계획 대비 잔여갭 자가감사

- 구현계획 대비 자가감사에서 **미완 2건** 발견 → 수정 후 재배포.
  - whatif SSOT 통일(`_predict` 수량×단가, 구 `predict_revenue_per_m2` 디커플 1,221만↔3,195만 갈림 해소)
  - f7 필지합=총계(공동단가 기준 통일)
- **SW v95→v96** bump. iwinv 재빌드 → **2경로 대조** `iwinv == domain == v96` ✅

## 🌆 저녁 (17:50경) — 온보딩·거래성 7화면 보완 (v97) & 배포

**입력**: `KAASA_Farmingsight_온보딩거래성화면_보완진단_1.md` 진단 리포트. 3개 탐색 에이전트로 **v96 실제 코드 검증** — 리포트 지적 일부는 이미 해결·사실무근 → **검증된 유효 항목만** 계획·구현.

| 화면 | 보완 내용 |
|------|-----------|
| C16·C22 | 영문 표준변수(`temp_internal`·`dr_pct_mean` 등) 한글화 — 기존 `variable_labels` 재사용 + 괄호제거·`title` hover 원문보존 |
| C22 | 42행 기능 매트릭스 `<details>` 섹션 접기(잠긴 기능 있는 섹션만 자동 펼침) |
| C1 | 첫 화면 8단계 개요·필수/선택 필드배지·건너뛰기 안내·도트 단계명 |
| C21 | 신청 상태 단일배지 → 3단계 스텝퍼(접수→검토→연동완료) |
| C15 | 교육 빈상태 UX — 소요시간·형식 배지·"준비중" 정직표기·첫 모듈 시작 CTA |
| C12 | 개별≈공동 단가 근접 시 중립 분기(비교 의미 낮음, win 제거) |
| C2 | "환경·관수·생육 기본 공개(익명 학습)" 고지문구 — 기본값 변경 없음(사용자 결정) |

- **스킵**(검증 결과 불필요): C1 재개보존(이미 구현)·C2 4모드 배타(정상)·C16 붙어쓰기버그(미재현).
- 전 화면 프런트(HTML)+`sw.js`만 변경 → 백엔드 무영향. 7화면 로컬 렌더·콘솔 에러 0 검증.

| 시각 | 커밋 | 내용 |
|------|------|------|
| 17:45 | `7f22c2b` | v97: 온보딩·거래성 7화면 보완(C1·C2·C12·C15·C16·C21·C22) + SW v96→v97 |

- **배포**: PC `git push` (`3bbbf20..7f22c2b`) → 서버 `git checkout origin/master -- screens sw.js` → `docker compose build api && up -d api`(healthy) → **2경로 대조** `iwinv:v97 == domain:v97` ✅
  - 화면 스팟체크(도메인): C15 "준비중"·C1 "8단계 온보딩 안내"·C21 "req-steps"·C2 "기본적으로" 실제 서빙 확인.

## 🎨 밤 — 가독성·레이아웃 수정 (v98) & 배포

모바일 스크린샷 2건(홈·랜딩) 지적 기반.

| 대상 | 원인 | 수정 |
|------|------|------|
| 다크모드 헤더·하단바 흰글씨 안 보임 | `--green-dark`가 다크서 밝은민트로 재정의되는데 헤더·하단바·드로어·사이드바가 이걸 **배경**으로 사용 → 대비 1.5:1 붕괴 | 배경전용 `--chrome-bg` 토큰 분리(양테마 어둡게, **대비 13.8:1**) |
| c3 헤더 텍스트 세로눌림 | 좁은폭 flex 축소 | WS상태·브랜드 `white-space:nowrap` |
| intro 상단 흰공백 | safe-area/주소창 영역에 밝은 `--bg` 노출 | `html` 루트 `#0c2a1c` 도색 + `theme-color` + hero safe-area 패딩 |
| intro 하단 CTA 겹침 | 반투명 sticky 그라디언트로 콘텐츠 비침 | 불투명 배경 + 구분선·그림자 도킹바 |

## 🔬 밤 — 모델 인과체인 진단 & 수리 (환경→수확·병해·소득)

**질문**: "환경→수확·병해·소득 영향 모델이 제대로 도나?" → 라이브 API 실측 + 탐색 에이전트 2대로 교차검증.

**진단 결론**: "서빙은 도는데 **환경→수확 인과가 대부분 끊겨 있다.**"
- M2 수확: 6종 중 신뢰가능 2종(딸기 CV R²0.291·오이 0.386)뿐. 오늘 재학습이 소표본 낙관편의를 정직값으로 붕괴(파프리카 0.802→0.076, 방울·참외 음수→폴백). 완숙·파프리카 conditional, 방울·참외 fallback.
- **환경→수확: 6종 중 5종 무반응** — `_env_to_feat`가 `*_mean`만 넘겨 무접미사 규약 모델(딸기·파프리카)의 환경 피처가 0.0으로 박제(imputer가 0.0을 결측 취급 안 함 → 딸기가 temp=0℃로 예측). + RDA 클리핑 + 완숙(growth/vpd 피처)·방울(std 피처)은 서빙 env로 못 움직임.
- 환경→병해: 규칙기반(온·습도·CO₂→VPD·Baud) 라이브 작동, M5 이미지모델은 스텁(미구축). 결로시간·일사·토양온도는 스키마엔 있으나 미사용.
- 환경→소득: 매출=수량×단가로 연동(정상)이나 수확이 환경 상수라 소득도 무반응 상속.

**수리(우선순위)**:

| # | 항목 | 조치 | 검증 |
|---|------|------|------|
| 1 | 피처명 정합 | `m2_yield._predict_format_c`에서 `*_mean`↔무접미사 상호 별칭(단일 초크포인트→harvest·revenue·whatif·erp·pdca 일괄). `*_std` 제외 | 파프리카 습도반응 복구·**오이 18000 SSOT 보존**·방울/완숙 불변 |
| 2 | 완숙토마토 stub | 에이전트가 `feature_cols=[]`라 했으나 **실제 로드 시 12피처 존재 → stub 아님** | 수정 불필요(헛수정 회피) |
| 3 | 병해 결로 반영 | `m5_disease._score_disease`에 결로(노점 근접, RH≥90) 위험 항목 추가·`wet_hours` 가중. 건조병해 제외 | RH95→결로 근거 표시·건조병해 제외 확인 |
| 4 | 로컬 uvicorn 재시작 | 21:50 기동 stale 프로세스(구코드: 42005·한글깨짐·pdca 500) 종료 후 현재 코드로 재기동 | harvest 18000·pdca 200·인코딩 정상 |

★ **에이전트 보고를 편집 전 코드로 재검증**해 Item 2 헛수정을 회피. 딸기·오이는 RDA 클립에 걸려 출력은 평평하나 모델 입력값(temp=0→실측) 교정됨 — 클립·성장피처 부재는 별도(재학습) 과제.

| 시각 | 커밋 | 내용 |
|------|------|------|
| 밤 | `5efda27` | v98 가독성·레이아웃(base.css `--chrome-bg`·c3·intro·sw.js v97→v98) |
| 밤 | `1de82d5` | 모델 인과체인 수리(m2_yield 피처 정합·m5_disease 결로) |
| 밤 | `afe6bcc` | docs 작업일지 v96·v97 |

- **배포**: PC push(`7f22c2b..afe6bcc`) → 서버 **6개 파일만 선별** checkout(`models/`는 registry·artifacts 클로버 방지로 통째 금지·변경 py 2개만) → `build api && up -d`(healthy) → **2경로 대조** `iwinv:v98 == domain:v98` ✅. 도메인 백엔드 스팟: `오이 18000`·disease-risk 정상.

## 🌱 심야 — 환경→수확 근본 복구 진단 & 정직 재설계 (v99)

**요청**: 남은 과제였던 "환경→수확 근본 복구" 착수.

**진단(핵심)**: 이미 존재하던 `scripts/train_env_response.py`(환경만으로 수확 설명, lag·이력·생육 제외한 최적화 전용 모델)를 실행 → **CV R²가 전 작목 음수**(딸기 −0.49·오이 −0.87·파프리카 −1.20·완숙 −0.24·방울 −1.39). 반응폭(6~76%)은 커 보이나 일반화 안 되는 모델이 교란(농가 실력)에 과적합한 노이즈. **결론: 관측 데이터로 환경→수확 인과는 신뢰 복구 불가.**
- 클리핑(딸기 1.5·오이 15.0 하한)은 근본원인이 아니라 증상 마스커 — 풀면 쓰레기값 노출. 근본은 train-serve 피처 스큐 + 데이터에 환경 신호 부재.
- 사용자 결정: **정직한 농학기반 재설계**(노이즈 모델을 what-if에 배선하지 않음 — 코드베이스 '정직화' 원칙 유지).

**재설계**: what-if를 '수확량 델타 예측' → **농학적 최적범위(crop_config opt_range) 이탈도 + M4 실비용 + 수확 영향 한계 고지**로 전환.
- `schemas/farmer.py` WhatIfResult에 `agronomic`·`yield_effect_note` 추가
- `routers/farmer.py` `_agronomic_assessment`(최적범위 이탈 판정) + `/whatif` 배선, 비용(M4) 유지
- `g4_growth.html`: "예상수확 kg·신뢰도 75% ML" 과신 제거 → 밴드 평가·비용·caveat. 프리셋 비교도 가짜 매출랭킹→비용·밴드이탈 비교. 배지 🔵모델→🔶농학기준·비용
- 브라우저 실측: 12℃→"최적 22~30℃ 하한 미만(생육 저하)"·비용 +32.5만·"수확 영향 신뢰예측 불가" 표기, 콘솔 0

**intro 여백·겹침 재수정(재발 지적)**: v98 상단갭 수정이 실패했던 이유 규명 — **진짜 원인은 base.css 고정헤더(52px)·하단탭(60px) 오프셋 body 패딩**이 헤더·탭 없는 랜딩에 빈칸으로 남던 것(v98의 html 배경 도색은 overscroll 방어로만 유효했음, safe-area로 **오진**). intro `body{padding:0}` 리셋으로 해결. 하단 CTA 겹침은 v98 불투명 도킹 + 상단갭 제거 상향으로 해소. ★ 데스크톱만 검증해 모바일 갭을 놓친 게 오진 원인 — 이번엔 모바일 뷰포트로 실측(hero_top 0).

| 시각 | 커밋 | 내용 |
|------|------|------|
| 심야 | `658c470` | docs 작업일지 v98·모델수리 (별도 push) |
| 심야 | `de04efe` | 환경→수확 정직 재설계(schemas·routers·g4 — agronomic·yield_effect_note) |
| 심야 | `df04c73` | v99: intro 상단빈칸·하단겹침 수정 + SW v98→v99 |

- **배포**: PC push(`658c470..df04c73`) → 서버 **5개 파일만 선별** checkout → `build api && up -d`(healthy) → **2경로 대조** `iwinv:v99 == domain:v99` ✅. 도메인 스팟: intro `padding 0`·g4 "농학기준·비용"·`/whatif` agronomic 라이브.

---

## 요약 지표
- **커밋 23개** (코드 19 + 문서 3), 11:26~심야
- **SW v81 → v99** 운영 반영(v95·v96·v97·v98·v99 5회 배포), farmingsight.org 2경로 대조 검증 완료
- **경제지표 전 화면 단일값 정합**: 오이 18,000kg·1,690원/kg·3,042만원·48% (홈·c5·g4·g6·c12·c25 동일)
- 모델 진단: 신뢰가능 M2 수확모델 2종(딸기·오이)·**환경→수확 인과는 데이터로 신뢰 복구 불가(env_response CV R² 전작목 음수) → what-if 농학기반 정직 재설계**·병해 규칙기반+결로 반영·소득 배선 정상

## 남은 항목 (별도 과제 · 재학습/판단)
- ~~**환경→수확 근본 복구**~~: **진단·재설계 완료(v99)** — 데이터로 인과 신뢰 복구 불가 확인, what-if 농학기반 정직 재설계. 진짜 인과 확보는 **무작위 배정 실험/센서 실측 데이터 축적**이 있어야 가능(현 관측데이터 한계). 재도전은 데이터 확보 후.
- **병해**: 실측 결로시간(엽면습윤 센서)·M5 이미지모델 구축
- C0-3 비밀번호 최소 4자(보안 정책)·랜딩 정량 마케팅 수치 L1(영업 판단)·데모 WS 401 retry(기존 인프라)

## 남은 항목 (코드 정합 아님 · 정책/판단)
- C0-3 비밀번호 최소 4자(보안 정책 결정)
- 랜딩 정량 마케팅 수치 L1(영업 판단)
- 데모 토큰 WS(`/ws/farms/{id}/sensors`) 401 retry 루프(기존 인프라)
- (문서 커밋 `13e2180` origin 미반영 — 원하면 `git push origin master` 1회)
