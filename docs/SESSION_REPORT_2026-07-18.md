# 세션 마무리 보고 — 2026-07-18

> **한 줄**: 튜닝은 신기루, 지렛대는 데이터.
> 최종 커밋 `562a7f6` · GitHub·로컬·iwinv 서버 동기화됨 · 서빙 변경 SW v81→v82
> 아티팩트(시각본): claude.ai/code/artifact/cb62fd9b-7ffc-44bc-a9d7-5dcd16ff68db

| 지표 | 값 |
|------|-----|
| 커밋 (전부 push·동기화) | 7 |
| 서빙 배포 (컨테이너 rebuild) | 1 |
| M2 판정 (적용·조건부·폴백) | 2 · 2 · 2 |
| 모델 판정 변경 | 0 (정직 유지) |

---

## 1. 운영 배포 — iwinv, farmingsight.org  ✅

- 진입 3화면(index·intro·console) 푸터에 문의 이메일 `iiam@iiam.co.kr` 추가, 서비스워커 **v81→v82**.
- **3경로 대조 검증** — 컨테이너 실체 · `localhost:8000` · 공개 도메인이 모두 v82로 일치(터널 하이재킹 없음).
  Cloudflare 이메일 난독화로 `curl`엔 안 보이나 실브라우저는 정상 복원.
- 프론트는 이미지 베이크라 rebuild, 아티팩트는 named volume이라 `docker compose cp` 필수 — 함정 회피하며 반영.
- 커밋 `a91db14`(유일한 서빙 배포).

## 2. 중간보고서 — 이암허브 2026 (1단계 2년차)  ✅

- 제6장 **환경제어 실증 설계 구체화** — 평가의견 "방법론의 구체적 제시"에 응답:
  처치 요인·수준(DIF·CO₂·VPD), 농가 내 구획 배치·반복, 검정력 기반 표본 산정(f²≈0.06 → 150~250 관측)으로 「기재 필요」 대체.
- 배포 3경로 검증·문의 채널 실적 반영. canonical 갱신, `_20260718.docx` 스냅샷 보존.
- 산출 docx는 `out/`(gitignore) — 스크립트만 커밋(`f3269c7`·`cf22e2d`).

## 3. ★ 모델 정직 진단 — 이 세션의 핵심

파프리카·완숙 M2를 정직하게 튜닝했다. 모드 선택을 CV MAPE→CV R²로 바꾸자 참외가 **−0.122 → 0.506**으로 뛰었으나,
그 R²의 정체는 `yield_lag1`·`yield_ewm3`(지난달 수확)·`month_sin/cos`(계절모양)·`farm_yield_hist_mean`(농가정체성)뿐 —
**환경·경영 변수는 하나도 없었다.** 서빙/계획 시점엔 지난달 실측이 없어 못 쓰는 **자기상관 신기루**.

→ 코드 튜닝의 실질 개선 **0**. 되돌리고 함정을 코드 주석 + 재현 스크립트 `scripts/_tune_m2_experiment.py`로 고정(`0aef88e`).
부산물: 배포 아티팩트가 stale이나, 순진한 재학습은 MAPE 모드선택이 R²-음수 annual을 골라 오히려 악화 → 함부로 재학습 금지.

### M2 stage2 판정 (CV R² 게이트 · `gates.py` MIN 0.20 · FALLBACK 0.0)

| 작물 | CV R² | 판정 | 비고 |
|------|-------|------|------|
| 오이 | 0.386 | 서비스 적용 | annual · n=61(취약) |
| 딸기 | 0.291 | 서비스 적용 | monthly · n=6427 |
| 파프리카 | 0.076 | 조건부 | monthly · n=858 |
| 완숙토마토 | 0.099 | 조건부 | annual · n=177 |
| 참외 | −0.122 | 폴백 | 표본 84 · 피처 64 |
| 방울토마토 | −0.257 | 폴백 | n=103 · 신호부족 |

게이트 재편 이후 판정 **불변** — "개선"은 전부 자기상관 아니면 stale 아티팩트 착시였다.

## 4. 진짜 지렛대 — 외부 데이터  ✅

`docs/DATA_ACQUISITION_DESIGN.md`(신규) — 같은 환경센서 축적은 무효(신호 0.12~0.19·교란). **외부 입력 우선순위**:

1. **품질·등급**(당도·특품율) — 매출 오차 주범인 단가를 결정. 지금은 kg/m² 수량만 예측.
2. **농가 실비용 13항목** — 소득(매출−비용) 검증이 비로소 가능.
3. **가격 선행지표**(도매 반입량·수입량·작황) — KAMIS는 결과만 줌.
4. **근권·양액 실측** · 5. **품종·정식일** · 6. **병해 라벨**.

다섯이 **과업①(소득조사 20개소)**과 겹친다. 환경축만은 외부입력이 아니라 **처치 실험**만이 해법.
커밋 `cbcd794`·`13d6268`.

## 5. NEXT.md · 작업 메모리  ✅

세션 결론 반영. stale 정정 — 모델 표(참외 27.3% "실용" 오정보) → CV R² 판정으로 교체, SW v73→v82,
그리고 **이 PC에서 cloudflared 실행하라는 위험 지시**(터널 하이재킹 원인) 제거. 커밋 `562a7f6`.

---

## 잔여 — 사용자 액션 필요

| 우선 | 항목 | 내용 |
|------|------|------|
| 🔴 | **저장소 비공개 전환** | `github.com/smartyoung25/smart1` 2026-07-18 재확인 결과 여전히 **공개**. git 이력에 연락처 잔존 — 사용자만 전환 가능(gh 미인증). |
| 🔴 | **외부 데이터 확보** | 모델 성능의 실질 지렛대. 품질·등급 → 농가 실비용 → 가격 선행지표. 상세 `docs/DATA_ACQUISITION_DESIGN.md`. |
| 🟡 | **환경 실증 실험 설계 협의** | 농진청. 관측으로는 환경효과 식별 불가 — 처치 배정만이 해법(보고서 6장). |
| 🟡 | **인프라 키** | ANTHROPIC 크레딧 충전(챗봇 LLM off) · `.env` SMTP/Slack/CoolSMS · Let's Encrypt(현재 Cloudflare 터널 TLS). |

## 이 세션 커밋 (7)

| 커밋 | 내용 | 서빙 |
|------|------|:---:|
| `a91db14` | 푸터 문의 이메일 · SW v82 | ✅ 배포 |
| `f3269c7` | 중간보고서 배포 3경로 검증·문의채널 | — |
| `cf22e2d` | 중간보고서 제6장 환경실증 설계 | — |
| `0aef88e` | M2 튜닝 실험 인프라 + 자기상관 경고 | — |
| `cbcd794` | docs 데이터확보 설계 문서 | — |
| `13d6268` | docs 외부입력 우선순위 절 | — |
| `562a7f6` | NEXT.md 세션 결론 반영 | — |

> 다음 세션은 `NEXT.md`의 "★ 이번 세션 결론"부터 읽을 것.
