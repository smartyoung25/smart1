# 스마트팜 AI 플랫폼 — 현장 디지털트윈 매핑 (Phase 41)

> 작성일: 2026-05-23  
> 버전: Phase 41 (M1 6/6 PASS, M2 5/6 PASS, M3/M4 6/6 PASS)

---

## 1. 현재 모델 성능 지표 (Phase 41 재학습 결과)

### M1 — 환경 → 생육 예측

| 작목 | n_train | 모델 | CV R² | Train RMSE | Train MAE | Gate |
|------|---------|------|-------|-----------|----------|------|
| 딸기 | 1,912 | XGBoost | 0.242 | 측정 중 | 측정 중 | ✅ PASS |
| 방울토마토 | 1,005 | Ridge | **-0.187** | 측정 중 | 측정 중 | ✅ **PASS (수정)** |
| 완숙토마토 | 1,721 | Ridge | -0.044 | 측정 중 | 측정 중 | ✅ PASS |
| 파프리카 | 1,144 | XGBoost | -0.010 | 측정 중 | 측정 중 | ✅ PASS |
| 참외 | 596 | XGBoost | 0.241 | 측정 중 | 측정 중 | ✅ PASS |
| 오이 | 399 | XGBoost | 0.094 | 측정 중 | 측정 중 | ✅ PASS |

> **Phase 41 개선:** 방울토마토 M1 EWM 조건부 비활성화 → min_fold=-0.684→-0.023, gate FAIL→**PASS**

### M2 — 생육 → 수확량 예측 (kg/m²)

| 작목 | n_train | 모델 | CV R² | Train MAPE | Train RMSE | Train MAE | Gate |
|------|---------|------|-------|-----------|-----------|----------|------|
| 딸기 | 205 | XGB+LGB 앙상블 | -0.150 | 50.2% | 측정 중 | 측정 중 | ✅ PASS |
| 방울토마토 | 42 | Ridge fallback | 0.617 | 45.5% | 측정 중 | 측정 중 | ✅ PASS |
| 완숙토마토 | 177 | Ridge CV | 0.196 | 65.0% | 측정 중 | 측정 중 | ✅ PASS |
| 파프리카 | 112 | LGB | -0.191 | 64.5% | 24.3 | 16.9 | ❌ FAIL (소표본) |
| 참외 | 84 | Ridge CV | -0.105 | 45.3% | 측정 중 | 측정 중 | ✅ PASS |
| 오이 | 27 | Ridge fallback | 0.856 | 35.7% | 14.6 | 10.5 | ✅ PASS |

> **파프리카 M2 FAIL**: n=112 연간 데이터로 CV MAPE=127.8%, R²=-0.191. Gate가 올바르게 배포 차단 중 (안전 장치 정상 작동). 현장에서는 다년도 데이터 추가 수집 필요.

### M3 — 매출 예측 / M4 — 비용 산정

| 계층 | 방식 | 6작목 | 비고 |
|------|------|-------|------|
| M3 매출 | 수확량×aT도매가×면적×World Bank 보정 | 6/6 ✅ | 실시간 가격 API 연동 |
| M4 비용 | 모수형 (난방·비료·인건비·농자재) | 6/6 ✅ | 농가별 입력값 |
| M5 병해 | Rule-based stub (EfficientNet 미확보) | 6/6 stub | AIHub 이미지 신청 후 교체 |

---

## 2. 현장 디지털트윈 전체 구조도

```
┌────────────────────────── 물리 현장 (Physical Farm) ─────────────────────────┐
│                                                                               │
│  [온실 내부]                                [외부]                            │
│  온도센서(PT100) ──────────────────────────  기상스테이션                     │
│  습도센서(SHT31) ──────────────────────────  NASA POWER API (보완)            │
│  CO₂센서(SenseAir) ─────────────┐           외기온도·강수량                   │
│  일사센서(LI-COR) ───────────────┤                                            │
│  토양온도(DS18B20) ──────────────┤           [모니터링]                       │
│  토양수분(FDR) ──────────────────┤           IP카메라 → M5 병해탐지           │
│  EC센서 ────────────────────────┘                                            │
│  관수장치(드립/분무) ──────────────── 자동화 가능                              │
│  CO₂ 공급장치 ─────────────────────── 자동화 가능                             │
│  난방장치 ──────────────────────────── 자동화 가능                            │
│                                                                               │
└─────────────────────────────────┬─────────────────────────────────────────────┘
                                  │ MQTT (5초 주기 → 월집계)
                                  ▼
┌─────────────────── 데이터 계층 (PostgreSQL 17) ────────────────────────────────┐
│                                                                               │
│  env_measurements(IoT원시)  growth_records(주간측정)  yield_records(연간)     │
│  temp/humidity/co2/solar    초장·엽수·엽장·줄기굵기   kg/m²·면적·품종        │
│                                                                               │
│  irrigation_logs            cost_records               price_records          │
│  관수량·EC·pH              난방비·비료비·인건비        aT도매가·시장가격       │
│                                                                               │
└───────────────────────────────────┬───────────────────────────────────────────┘
                                    │ 월집계 → 학습 행렬 구성
┌─────────────────── ML 파이프라인 (AI 엔진) ────────────────────────────────────┐
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │  M1 생육예측 (env_lag2 → growth_features)                           │     │
│  │                                                                     │     │
│  │  입력 피처:                          출력 (작목별):                  │     │
│  │  · temp_internal_mean + lag1/lag2   · 초장(cm)                     │     │
│  │  · humidity_int_mean + lag1/lag2    · 엽수(개)                     │     │
│  │  · co2_ppm_mean + lag1             · 엽장·엽폭(cm)                │     │
│  │  · solar_rad_mean → dli_est        · 줄기굵기(mm)                  │     │
│  │  · gdd_monthly → gdd_cumsum        · 화방착과수(개)                 │     │
│  │  · vpd_kpa (Magnus 공식)           · 개화·착과 마디                 │     │
│  │  · temp_x_solar (광합성 coupling)  │                               │     │
│  │  · month_sin/cos (계절성)          │                               │     │
│  │                                    │                               │     │
│  │  모델: XGBoost (딸기/파프리카/참외/오이)                            │     │
│  │        Ridge (완숙토마토/방울토마토 — 안정성 우선)                   │     │
│  │  CV: TimeSeriesSplit 4-fold (≥200샘플)                             │     │
│  └──────────────────────────────────┬──────────────────────────────────┘     │
│                                     │ 예측 생육 피처 → M2 입력                │
│  ┌──────────────────────────────────▼──────────────────────────────────┐     │
│  │  M2 수확량예측 (growth + yield_lag → kg/m²)                         │     │
│  │                                                                     │     │
│  │  입력 피처:                          출력:                           │     │
│  │  · M1 출력 (생육 예측값)            · yield_per_m2 (kg/m²)         │     │
│  │  · yield_lag1/2/3 (전년도 수확)     · P10 ~ P90 불확실성 구간       │     │
│  │  · yield_chg_rate (변화율)                                          │     │
│  │  · yield_ewm3 (EWM 트렌드)         임계이벤트 피처:                  │     │
│  │  · farm_yield_hist_mean/cv         · cold_stress_days (개화기 저온) │     │
│  │  · critical_cold/heat_days         · heat_stress_days (착과기 고온) │     │
│  │  · gdd_cumsum                      · vpd_stress_hours               │     │
│  │                                                                     │     │
│  │  모델: XGB+LGB 앙상블 (딸기)        Gate: MAPE≤110% OR R²≥0.0      │     │
│  │        Ridge fallback (소표본)      실패 시: 전작목 기준 중앙값 제공  │     │
│  └──────────────────────────────────┬──────────────────────────────────┘     │
│                                     │                                        │
│  ┌────────────────────────────────┐  │  ┌──────────────────────────────┐    │
│  │  M3 매출예측                   │  │  │  M4 비용 산정                │    │
│  │                                │  │  │                              │    │
│  │  수확량(kg) × 면적(m²)        │  │  │  난방비(°C·h × 단가)         │    │
│  │  × aT 실시간 도매가(원/kg)    │  │  │  비료비(kg/m² × 단가)        │    │
│  │  × World Bank 가격 보정계수   │  │  │  인건비(시간 × 임금)          │    │
│  │  → 연간 예상 매출액(원)       │  │  │  농자재비(m² × 단가)          │    │
│  └─────────────────┬──────────────┘  │  └──────────────┬───────────────┘    │
│                    └─────────────────┤─────────────────┘                    │
│                                      │ 매출-비용 = 순수익                    │
│  ┌───────────────────────────────────▼─────────────────────────────────┐    │
│  │  Profit Optimizer (Optuna TPE Bayesian — 60 trial)                  │    │
│  │                                                                     │    │
│  │  목적함수: max(M3 예상매출 - M4 예상비용)                             │    │
│  │  결정변수: 온도 set-point · CO₂ ppm · 습도 · 관수량                  │    │
│  │  출력: Recommendation 리스트 (rank/action_ko/profit_delta)           │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │  M5 병해탐지 (현재: Rule-based stub)                                 │     │
│  │  · 카메라 이미지 → EfficientNet-B3 (AIHub 이미지 확보 후 교체)       │     │
│  │  · 현재: 이상 환경값(습도>90%, 온도편차>5°C) → 경보                  │     │
│  └─────────────────────────────────────────────────────────────────────┘     │
└────────────────────────────────────────┬──────────────────────────────────────┘
                                         │
┌────────────────────────────────────────▼─────── API / 대시보드 ───────────────┐
│  FastAPI (포트 8000)                                                          │
│                                                                               │
│  GET  /api/growth          → M1 생육 예측 (월별 생육 지표)                    │
│  GET  /api/harvest         → M2 수확량 예측 + P10/P90 신뢰구간               │
│  GET  /api/revenue         → M3 매출 예측 (면적·도매가·보정계수)              │
│  GET  /api/costs           → M4 비용 조회 (항목별 상세)                       │
│  POST /api/costs/manual    → 수동 비용 입력                                   │
│  GET  /api/optimize        → Bayesian 환경 최적화 권고                        │
│  POST /api/disease/detect  → M5 병해 탐지 (이미지 업로드)                    │
│  GET  /api/market/wholesale → aT 실시간 도매가                                │
│  POST /api/irrigation      → Priva 관수 스케줄 수신/제어                     │
│  POST /api/chat            → AI 채팅 (Claude/GPT-4o)                         │
│  GET  /api/what-if         → 환경 시뮬레이션 (What-if 분석)                  │
│  WS   /ws/realtime         → 실시간 환경 모니터링 (WebSocket)                │
│                                                                               │
│  대시보드 (포트 5500)                                                         │
│  · 생육/수확/매출 예측 탭  · 실시간 환경 모니터링 탭                          │
│  · 이상 감지 알림          · 비용/수익 분석 탭                                │
│  · 최적화 권고 카드        · AI 채팅 탭                                       │
└────────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 작목별 센서 → 모델 입력 매핑

| 물리 센서 | DB 컬럼 | M1 피처 | M2 피처 | 현장 장비 권장 |
|----------|--------|--------|--------|------------|
| 내부 온도 | temp_internal | _mean, _lag1, _lag2, vpd_kpa | cold_stress_days | PT100 / DS18B20 |
| 내부 습도 | humidity_int | _mean, _lag1 | — | SHT31 / DHT22 |
| CO₂ 농도 | co2_ppm | _mean, _lag1 | — | SenseAir S8 / Sensirion SCD30 |
| 일사량 | solar_rad | _mean → dli_est, dli_ewm3 | — | LI-COR LI-200R |
| 토양온도 | soil_temp | _mean → gdd_monthly | gdd_cumsum | DS18B20 방수형 |
| 외기온도 | temp_external | (NASA POWER 결측 보완) | — | 외부 기상스테이션 |
| 토양수분 | soil_moisture | _mean | — | FDR/TDR 센서 |
| 카메라 | image | — | — | Raspberry Pi HQ Camera (M5) |

---

## 4. 작목별 디지털트윈 임계이벤트 및 최적 환경

| 작목 | M1 타겟 생육지표 | 주요 임계이벤트 | 최적 환경 범위 | Bayesian 최적화 목표 |
|------|--------------|--------------|------------|-----------------|
| 딸기 | 초장·엽수·화방착과수 | 화아분화(9~10월 저온 8°C 이하) | 15~25°C, VPD 0.5~1.0kPa, DLI 8~12 | 겨울 야간 15°C 유지 |
| 방울토마토 | 초장·착과수·화방꽃수 | 낙화(35°C↑), 수정장애(10°C↓) | 20~28°C, CO₂ 800~1200ppm | CO₂ 증시 수익성 분석 |
| 완숙토마토 | 초장·착과군수 | 배꼽썩음(Ca결핍·물스트레스) | 18~26°C, EC 2.0~3.5dS/m | 관수 EC 최적화 |
| 파프리카 | 초장·화방높이·착과마디 | 낙화(저온·고습 동시) | 20~28°C, 습도 70~80% | 습도 제어 난방 연동 |
| 참외 | 초장·줄기굵기 | 착과기 온도(5~6월 25°C 이상) | 25~30°C, 주야온도차 10°C | 주야 온도차 관리 |
| 오이 | 초장·마디수·엽수 | 개화기 냉해(12°C 이하) | 22~28°C, VPD 0.7~1.3kPa | 봄철 보온 수익화 |

---

## 5. 데이터 흐름 및 갱신 주기

```
[IoT 센서]                [DB 저장]              [모델 예측]            [대시보드]
 5초 원시값    →   MQTT   → env_measurements  →  실시간 이상감지   →   알림 표시
 (실시간)                  (원시 보존)             (WebSocket)

 일별 집계     →   ETL    → env_daily        →   —                 →   환경 트렌드

 월별 집계     →   ETL    → env_monthly      →   M1 생육 예측      →   생육 카드
 (학습 입력)              + growth_monthly        M2 수확량 예측         수확 예측
                                                  M3 매출 예측           매출 예측

 연간 집계     →   수확 후 → yield_records   →   M2 재학습 트리거  →   모델 갱신 알림
 (학습 갱신)              + cost_records         (새 데이터 ≥10건)
```

---

## 6. 현장 활용 수준 달성 로드맵

### 현재 수준 (Phase 41)

| 지표 | 현장 목표 | 현재 최고 | 현재 최저 | 비고 |
|------|---------|---------|---------|-----|
| M1 CV R² | ≥ 0.30 | 0.242 (딸기) | -0.187 (방울토마토) | 6/6 gate PASS |
| M2 Train MAPE | ≤ 30% | 35.7% (오이) | 65.0% (완숙토마토) | 5/6 gate PASS |
| M2 CV R² | ≥ 0.40 | 0.856 (오이) | -0.191 (파프리카) | |
| M3 예측 오차 | ≤ 20% | MAPE 연동 | | 도매가 정확도 의존 |

### 향후 개선 과제 (Phase 42+)

| 우선순위 | 과제 | 예상 효과 |
|---------|-----|---------|
| 1 | 파프리카 M2 추가 데이터 수집 (n≥200 목표) | gate PASS → 5/6→6/6 |
| 2 | M5 병해탐지 EfficientNet 실 모델 (AIHub #535) | stub→실 모델 |
| 3 | NASA POWER 위성 기상 M1 결측 보완 자동화 | 기상 데이터 품질 향상 |
| 4 | SoilGrids API 토양 특성 초기화 | 참외·오이 M1 개선 |
| 5 | 완숙토마토 M2 QuantileRegressor 추가 | MAPE 65%→45% 목표 |

---

## 7. 운영 체크리스트

```powershell
# 일별 모델 헬스체크
C:\tools\python311\python.exe -c "
import json, glob, sys
fails = []
for f in sorted(glob.glob('models/artifacts/*/stage?.meta.json')):
    d = json.load(open(f))
    if not d['gate_passed']:
        fails.append(f'{d[\"crop_ko\"]} Stage{d[\"stage\"]}')
print('Gate FAIL:', fails if fails else '없음 (전작목 PASS)')
"

# 전체 테스트
C:\tools\python311\python.exe -m pytest tests/ --tb=no -q 2>&1 | tail -3

# API 헬스체크
curl http://localhost:8000/health

# 실시간 모니터링 상태
curl http://localhost:8000/api/env/latest?farm_id=farm_001
```

---

*이 문서는 Phase 41 완료 시점의 디지털트윈 설계를 반영합니다.*  
*업데이트: Phase 완료 시마다 성능 지표 섹션을 갱신하세요.*
